class Order {
  int id;
  String name;
  int totalDestination;
  int price;
  int total;
  String notes;
  String category;

  Order({this.id, this.name, this.totalDestination, this.price, this.total, this.notes, this.category});
}