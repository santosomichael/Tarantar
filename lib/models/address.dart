class Address {
  int id;
  double rating;
  String name;
  String phone;
  String address;
  double latitude;
  double longitude;

  Address({this.id, this.rating, this.name, this.phone, this.address, this.latitude, this.longitude});
}