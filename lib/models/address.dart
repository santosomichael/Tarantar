class Address {
  int id;
  String name;
  String phone;
  String address;
  String latitude;
  String longitude;

  Address({this.id, this.name, this.phone, this.address, this.latitude, this.longitude});
}